name: Push File 

on: 
  pull_requests:
    types: opened

permissions:
  pull-requests: write

jobs:
  Pushing-Text-Files: 
    runs-on: ubuntu-latest
    steps:
      - run: echo "Treinando integração continua."
      - run: echo "Bom dia"

